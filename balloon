import turtle
import time
import random

scr = turtle.Screen()
scr.bgcolor("purple")
scr.setup(700,600)
#To turn off the screen update
#scr.tracer(0)

#player setup
p = turtle.Turtle()
p.shape("triangle")
p.color("yellow")
p.penup()
p.goto(0,-270)
p.down()


p.setheading(90)

#score display
score = 0
score_display = turtle.Turtle()
score_display.hideturtle()
score_display.penup()
score_display.goto(-380, 260)
score_display.color("orange")
score_display.write(f"Score: {score}", font=("Arial", 16,"normal"))

# Balloon list
ballons = []

#game variables
game_speed = 0.02
difficulty_increase = 0.001
spawn_interval = 2
lst_spawn_time = time.time()
running = True


turtle.done()
